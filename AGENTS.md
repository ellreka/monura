# AGENTS.md

このファイルはコーディングエージェントがこのリポジトリで作業する際のガイドです。

## プロジェクト概要

マークダウン自由記述型のタスク管理デスクトップアプリ（Tauri製）。

**理念（実装判断に迷ったら常にここに立ち返る）:**

0. **アプリは必須ではない** — `.md` ファイルは任意のエディタで作成・編集でき、このアプリなしでも完全に成立する。アプリはファイルの上に乗る便利なレンズ（タイマー・表示・集計）にすぎない。アプリ固有の状態や機能がなければ意味が壊れる設計は禁止。
1. **マークダウンで自由に書ける** — タスクは特別なエンティティではなく、ただのチェックリスト行。通常のテキストメモと混在できる。ユーザーを「タスク」という単位に縛らない。
2. **プレーンテキスト至上主義** — マスターデータは素の `.md` ファイル。他のエディタで開ける・grepできる・Gitで管理できる状態を絶対に壊さない。独自DBや不可視メタデータの埋め込みは禁止。
3. **短いスパンの計測** — タイマーはプリセット時間（1h / 30m / 10m など）を選んで開始する。短いスパンで区切って計測することでサボりを防ぎ、空白時間を可視化する。記録するのは実績のみで、見積もりの入力・記録は行わない。

## コア設計判断（変更にはユーザーの明示的な合意が必要）

### タスクの認識

- チェックリスト形式の行（`- [ ]` / `- [x]`）のみをタスクとみなす
- それ以外の行はすべて自由記述のメモであり、アプリは一切干渉しない

### タスクの階層構造

```markdown
- [ ] 親タスク spent:20m
  - [ ] 子タスクA spent:15m
  - [x] 子タスクB spent:10m
```

- マークダウンのインデント（ネストしたリスト）をそのまま階層として解釈する。専用記法は追加しない
- **ファイルに書き込む時間データは常に「実際に計測した行」のみ**。親行への子の合計値の書き込み・同期は行わない（二重管理は必ず不整合を生む）
- 親の集計値（自身の spent + 子孫の spent 合計）は**エディタ上の表示としてのみ**計算して見せる
- 親タスク自体も直接計測できる（子に分解する前に親で計測を始める使い方を許す）
- チェックボックスの親子連動（子が全完了で親を自動チェック等）は**行わない**。アプリがファイルを書き換えるのは、計測終了時の `spent:` 追記と、ユーザーの明示的な操作によるチェックボックス切り替えのみ

### 完了タスクの扱い

- 完了（`- [x]`）タスクは**常にエディタ上に表示する**。非表示・折りたたみは行わない
- チェックボックスはエディタ上でクリックしてトグルできる。これはユーザーの明示的操作であり、自由編集の一部とみなす
- アーカイブ機能（done.txt への移動、別セクションへの自動移動）は**作らない**。ファイル内の整理はユーザーが手で行う
- 完了済みタスクへのタイマー開始は許可する（手戻り対応のため）

### 時間データの記法（todo.txt風 key:value）

```markdown
- [ ] APIのエラーハンドリング直す spent:25m
- [x] READMEを書く spent:15m
      今日は集中できなかったのでメモ。午後から仕切り直す。
```

- `spent:` — 実績時間。**加算方式**：常に累計値を1つだけ保持する。タイマー停止時に経過時間を既存値に加算して書き換える。`spent:` を複数並べる方式は採らない
- 時間の単位表記: `30m`, `1h`, `1h30m` 形式
- `est:`（見積もり）フィールドは**採用しない**。記録するのは事実（実績）のみ

### タスクの同一性（このプロジェクト最大の設計上の要点）

- **永続的なタスクIDは存在しない**。時間データはタスク行そのものに書き込まれるため、行が編集・移動されてもデータは行と一緒に動く。IDによる紐づけ・追跡は行わない
- **計測中のみ**、対象行をエディタ内部で追跡する（CodeMirror 6 の position mapping / state field を使用）。この追跡はメモリ内のみで、永続化しない
- 計測中の行のテキスト編集・前後への行追加は**自由**。position mappingで安全に追跡できる場合だけ追従する
- **追跡の有効性はタイマー停止時だけ検証する**。計測中に行の削除・移動・分割などで追跡先を確実に特定できなくなっても、タイマーを停止せず、リアルタイムの失敗警告も表示しない。停止時に `spent:` もログも自動確定せず、タイマーUIで「ログのみに記録」または「カーソル行に記録」をユーザーが選ぶ
- **計測先に少しでも不確実性がある場合、アプリは自動で `spent:` を付けない**
- 計測開始時にファイル名・行テキスト・開始日時・プリセットをスナップショットし、タイマー表示とログには停止時の行変更を反映しない

### セッションログ（二層構成）

- 行内の `spent:` は「現在の累計」のみ
- セッション履歴（開始日時・選択したプリセット時間・実際の経過時間・その時点のタスク行テキストのスナップショット）は**追記専用のログファイル**に別途記録する
- ログは歴史的事実として独立しており、タスク行が後で書き換わっても不整合とみなさない。行テキストは計測時点の値で焼き込む
- ログ形式は **JSONL**（1行1セッション）。人間が直接読むファイルではないため、集計しやすさを優先する
- ログは月次でローテーションする（例: `sessions-2026-08.jsonl`）
- **ログは「アプリを通じて計測されたセッション」の記録であり、`.md` の正しさはログに依存しない**。手動編集された `spent:` がログ集計と食い違っても不整合とみなさない（現在の状態は常に行内の値が正）。分析機能は「アプリで計測した時間の分析」と定義する
- **セッションログは分析・グラフ機能の唯一のデータソース**とする。`.md` 側の再パースに依存する集計設計にしない

### タイマー

- プリセット時間（1h / 30m / 10m など）から選択して開始する
- **停止時（手動停止・タイマー満了のどちらでも）、対象行を一意かつ安全に特定でき、外部競合がなければ、その時点までの経過時間を対象行の `spent:` に加算する**。それ以外はタイマーだけを停止し、タイマーUIで記録先を選ぶ。満了前の手動停止なら経過分のみが記録される
- システムトレイ / メニューバー常駐。満了時に OS 通知
- **満了検知とOS通知はRust側のネイティブアラーム（`alarm.rs`）が担う**。ウィンドウを閉じてトレイに常駐しているあいだ、WKWebViewはJSの `setInterval` をスロットリングするため、フロントエンド自身の満了ポーリングは非表示時に遅延しうる。Rust側でOSスレッドのスリープとして独立にスケジュールすることで、通知が表示状態に関わらず時刻どおりに届くことを保証する。`spent:` に何を書き込むかの判断はフロントエンドの責務のまま変えない（アラームはユーザーを起こすだけ）

### ファイル管理と外部編集

- ローカルファイルシステム上の `.md` を直接読み書きする（Rust側）
- ファイルウォッチで外部編集（Vim等）に追従する
- **外部編集は一級市民**: 手動での `spent:` の追記・修正、チェックボックスの切り替えはすべて正当な操作。ディスク上の内容は常に自動採用し、競合時は未保存のローカル内容を破棄する。**行内の値が「現在の状態」の唯一の真実**であり、アプリはログとの食い違いを検知しても行内の値を「修正」しない
- **計測中に外部編集を検知した場合**: タイマーは停止せずファイルをリロードする。対象行の再特定に失敗してもリアルタイムの失敗警告は表示せず、停止時の検証で記録先を選ぶフローへ合流する
- `spent:` トークンはエディタ上で控えめに表示する（小さくグレー等）が、ファイル内容はプレーンテキストのまま

### ファイルの長期運用（複数ファイル前提）

- **アプリは、ユーザーの明示的な削除操作以外で行を削除しない**。完了をトリガーとした自動削除・自動アーカイブ・自動移動は一切実装しない。行が消えるのは、ユーザーがその行そのものを削除したときだけ（アプリ内でもVim等の外部エディタでも同じ）
- 「完了タスクを一括削除する」ようなアプリ固有の掃除コマンドも**作らない**。アプリの有無で挙動が変わる機能は理念0に反する
- ファイルの成長は、**ユーザーが自由に `.md` ファイルを複数に分けること**で解決する。1ファイルで貫いても、増えてきたら新規ファイルを作っても、毎月分けても、すべて成立する
- **命名規則・ディレクトリ構造をアプリが規定しない**。月次ファイルの自動生成も行わない（アプリ固有の約束事を作らないため）
- アプリが提供するのは閲覧側の機能のみ: ディレクトリ内の `.md` の一覧・切り替え・新規ファイル作成
- ファイルが分割されても、セッションログがファイルを横断する唯一の歴史として機能する。月次でファイルを分けても年間の集計はログから出せる

### 三層の役割分担（迷ったらここに戻る）

| 層                     | 役割                                                  | 誰が変更するか                         |
| ---------------------- | ----------------------------------------------------- | -------------------------------------- |
| `.md` ファイル         | 現在の作業空間。今やること・書きたいこと              | ユーザー（分割・削除は完全に自由）     |
| セッションログ (JSONL) | 横断的な歴史。集計・グラフの唯一のデータソース        | アプリが追記のみ（削除・書き換えなし） |
| アプリ                 | 上記2つを見るレンズ（エディタ・タイマー・表示・集計） | —                                      |

## 実装済みの分析と将来拡張

- **実装済み**: セッションログ画面で、月別の合計時間、日別のセッション一覧・合計時間を表示する
- **将来拡張**: 日別推移やセッション間の空白時間など。データソースは引き続きセッションログ（JSONL）のみとする
- 記録し損ねたデータは後から復元できないため、新しい分析表示より先にログへ必要な事実を漏らさず記録する

## 技術スタック

- **Tauri v2**（デスクトップ）
- **CodeMirror 6**（エディタ。decorations / state fields / position mapping を活用）
- **TypeScript + React**
- **Vite**
- パッケージマネージャ: **pnpm**
- Rust側: ファイルI/O、ファイルウォッチ（notify crate）、トレイ、通知

## ディレクトリ構成

```
src/              # フロントエンド (TypeScript)
  components/     # React ビュー（Editor, Launcher, LogView, TimerBar, SettingsView）
  lib/            # 非UIモジュール（ロジック・Tauri IPC 境界層）
    editor/       # CodeMirror拡張（タスク認識、spentパース、装飾、計測中行の追跡）
    files/        # .md ファイルI/O（Tauri IPC の境界層。改行コード保持などの純粋関数を含む）
    log/          # セッションログのレコード定義と集計（純粋関数。LogView のデータソース）
    parser/       # spent: 記法のパース・シリアライズ（純粋関数、Tauri非依存）
    timer/        # タイマー状態管理
    settings.ts   # アプリ設定（データフォルダ・Vimモード・最後に開いたファイル）の永続化（tauri-plugin-store。セッションログと同じアプリデータディレクトリ）
src-tauri/      # Rust側
  src/commands.rs # .md / セッションログのファイルI/O（Tauri IPC の境界層）
  src/watch.rs    # ファイルウォッチ（notify crate）による外部編集の検知
  src/tray.rs     # トレイアイコン（計測中のみ表示。残り時間の表示・Stopメニュー）
  src/alarm.rs    # OS通知の起点となるネイティブの満了アラーム（WebView非依存。「タイマー」節を参照）
site/           # 紹介サイトとブラウザデモのビルド・配信
```

## 開発コマンド

```bash
pnpm tauri dev      # 開発起動
pnpm tauri build    # ビルド
pnpm test           # テスト (Vitest)
pnpm lint           # リント
pnpm --dir site build # 紹介サイトとブラウザデモのビルド
```

## 実装上の指針

- **コアロジックはTauri非依存に保つ**: パーサー、タイマーのドメインロジック、行追跡はブラウザでも動く純粋なTS/CodeMirrorコードとして書く。Tauri APIへの依存はファイルI/O・トレイ・通知の層に閉じ込める
- **パーサーは必ずユニットテストを書く**: `spent:` の抽出・加算・書き戻しはこのアプリの信頼性の核。エッジケース（重複した `spent:`、行末以外の位置、不正な時間表記）をテストで固める
- **`.md` ファイルの書き換えは最小差分で行う**: 対象行以外を絶対に変更しない。改行コード・末尾スペース・インデントを保持する
- **警告ダイアログは原則ゼロ**: 自由編集の思想に反するモーダルは避け、状態はタイマーUIやエディタ装飾で非破壊的に伝える

## コーディングルール

- コードコメントを追加してはいけない。`//`、`/* ... */`、JSDoc/TSDoc、TODO/FIXME、説明用コメントはすべて禁止する。
- ユーザーがコメント本文を明示的に要求した場合だけ例外とする。
- 既存コメントの削除・変更も、依頼された場合以外は行わない。

## やらないこと（スコープ外）

- クラウド同期・サーバーサイド機能（同期したければユーザーがGit/Syncthingを使う）
- **完了をトリガーとした行の自動削除・自動アーカイブ・自動移動**（アプリは行を勝手に消さない）
- アプリによるファイルの自動生成・命名規則の強制（月次ファイルの自動作成等）
- タスクの依存関係・優先度などのリッチなタスク管理機能（階層構造はインデントによる表現のみサポート）
- `.md` 以外へのデータ保存（IndexedDB等にマスターデータを持つこと）
- 行への不可視ID埋め込み
